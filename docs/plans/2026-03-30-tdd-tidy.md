# tdd-tidy 스킬 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** git diff로 변경된 Java 파일을 자동 탐지하여, tdd-blue agent의 8단계 Tidying Process를 TDD 사이클 없이 독립 실행하는 `/tdd-tidy` 스킬 추가

**Architecture:** 새 스킬(`skills/tdd-tidy/SKILL.md`)이 git diff를 분석해 대상 파일을 수집하고, 기존 `tdd-blue` agent를 standalone 모드로 호출한다. tdd-blue agent에 standalone 모드 분기를 추가하여 TDD 문서 의존 없이 동작하도록 한다.

**Tech Stack:** Claude Code plugin (markdown-based skills/agents)

---

## File Structure

| 파일 | 역할 | 변경 유형 |
|------|------|-----------|
| `skills/tdd-tidy/SKILL.md` | 독립 tidying 스킬 진입점 | Create |
| `agents/tdd-blue.md` | standalone 모드 분기 추가 | Modify |
| `.claude-plugin/plugin.json` | 버전 범프 | Modify |

---

### Task 1: tdd-blue agent에 standalone 모드 분기 추가

**Files:**
- Modify: `msbaek-tdd/agents/tdd-blue.md`

- [ ] **Step 1: tdd-blue.md의 GOAL 섹션에 standalone 모드 추가**

`## GOAL` 섹션의 맨 끝(기존 "다음 Red Phase 준비 완료" 아래)에 standalone 모드 설명을 추가한다:

```markdown
### Standalone 모드 (tdd-tidy에서 호출 시)

TDD 사이클 없이 독립적으로 호출될 때:
- 전달받은 **파일 목록**을 대상으로 Tidying Process 실행
- TDD 문서(SRS, 테스트 목록) 참조를 **스킵**
- "다음 Red Phase로 진행" 대신 **tidying 완료 후 종료**
- 커밋 메시지에 대상 파일 요약 포함
```

- [ ] **Step 2: CONSTRAINTS 섹션의 "Red-Green 다음에만" 규칙 완화**

기존:
```markdown
- **Red-Green 다음에만** - Never tidy during Red or Green phases
```

변경:
```markdown
- **Red-Green 다음에만** - Never tidy during Red or Green phases (standalone 모드 제외)
```

- [ ] **Step 3: OUTPUT FORMAT 섹션에 standalone 모드 작업 절차 추가**

`## OUTPUT FORMAT` 섹션의 맨 앞에 모드 분기 가이드를 추가한다:

```markdown
### 모드 판별

호출 시 전달된 컨텍스트를 확인하여 모드를 결정한다:
- **프로젝트 템플릿 문서 경로**가 전달됨 → RGB 모드 (기존 동작)
- **파일 목록 + "standalone"** 키워드가 전달됨 → Standalone 모드

### Standalone 모드 작업 절차

#### 1. 대상 파일 코드 냄새 식별
- 전달받은 파일 목록의 각 파일을 읽기
- 다음 패턴들을 찾기:
  - [ ] 깊은 중첩 (3단계 이상)
  - [ ] 중복 코드 (3회 이상 반복)
  - [ ] 사용하지 않는 코드
  - [ ] 비일관적 스타일
  - [ ] 긴 메서드 (20줄 이상)

#### 2. Tidying Process 적용
프로세스 흐름에 따라 순서대로 적용 (기존 8단계와 동일):
0~8단계 동일하게 적용

#### 3. 테스트 실행 및 검증
- 프로젝트의 테스트 프레임워크 자동 감지 (gradle/maven)
- 모든 기존 테스트가 통과하는지 확인
- 실패 시 변경사항 되돌리기

#### 4. 커밋 (변경이 있는 경우만)
- `git status`로 변경 사항 확인
- 변경이 없으면 "tidying 불필요 — 코드가 이미 깔끔합니다" 안내 후 종료
- 변경이 있으면:
  - `git add [변경된 파일들]` (git add -A 금지)
  - Write tool로 커밋 메시지 파일 생성 후 `git commit -F <파일>` 사용
  - 커밋 메시지 형식: `refactor: tidying [파일명 요약]`

#### 5. 완료 보고
- 적용한 tidying 단계 요약
- 변경 전/후 비교 설명
- 종료 (다음 Red Phase 안내 없음)
```

- [ ] **Step 4: 변경사항 확인**

Run: `git diff msbaek-tdd/agents/tdd-blue.md`
Expected: GOAL에 standalone 모드 추가, CONSTRAINTS 규칙 완화, OUTPUT FORMAT에 standalone 절차 추가 확인

- [ ] **Step 5: 커밋**

```bash
git add msbaek-tdd/agents/tdd-blue.md
```
Write tool로 커밋 메시지 파일 생성:
```
refactor(tdd): tdd-blue agent에 standalone 모드 분기 추가
```
```bash
git commit -F /tmp/commit-msg.txt && rm /tmp/commit-msg.txt
```

---

### Task 2: tdd-tidy 스킬 생성

**Files:**
- Create: `msbaek-tdd/skills/tdd-tidy/SKILL.md`

- [ ] **Step 1: skills/tdd-tidy 디렉토리 생성 확인**

Run: `ls msbaek-tdd/skills/`
Expected: tdd, tdd-plan, tdd-rgb 디렉토리 존재 확인

- [ ] **Step 2: SKILL.md 작성**

Create: `msbaek-tdd/skills/tdd-tidy/SKILL.md`

```markdown
---
name: tdd-tidy
description: git diff 기준 변경 파일을 자동 탐지하여 Composed Method 지향 Tidying Process를 독립 실행. /tdd-tidy로 호출.
argument-hint: "[commit-ref]"
---

# Tidying Skill — Composed Method 지향 독립 리팩토링

git diff로 최근 변경된 Java 파일을 자동 탐지하여, tdd-blue agent의 8단계 Tidying Process를 TDD 사이클 없이 독립 실행합니다.

## GOAL

- **성공 = 변경된 파일의 코드 냄새가 안전하게 제거되고, 모든 테스트가 통과하며, `refactor:` 커밋 완료됨**
- git diff 기준으로 대상 파일이 정확히 식별됨
- 8단계 Tidying Process가 적용됨
- 모든 기존 테스트가 통과함
- 하나의 `refactor:` 커밋으로 완료됨 (변경이 있는 경우)

## CONSTRAINTS

### Hard Rules
- **동작 변경 금지** — 구조 개선만 수행
- **테스트 수정 금지** — 구조 변경이 테스트를 깨면 되돌리기
- **80% 규칙** — 지금 할 수 있는 수준에서 80% 이하로 리팩토링
- **git add -A 금지** — 변경된 파일만 명시적으로 추가

## OUTPUT FORMAT

### 실행 절차

#### 1. 대상 파일 수집

인자가 전달된 경우 해당 commit ref와 비교, 없으면 unstaged + staged 변경 파일 수집:

```bash
# 인자 없음: unstaged + staged 변경 파일
git diff --name-only -- '*.java'
git diff --cached --name-only -- '*.java'

# 인자 있음: 특정 commit과 비교
git diff --name-only <commit-ref> -- '*.java'
```

- 테스트 파일(`*Test.java`, `*Tests.java`, `*Spec.java`)은 **제외**
- 변경 파일이 없으면: "tidying 대상 Java 파일이 없습니다." 안내 후 종료

#### 2. 대상 파일 확인

수집된 파일 목록을 사용자에게 보여주고 확인:

```
다음 파일에 Tidying Process를 적용합니다:
- src/main/java/com/example/OrderService.java
- src/main/java/com/example/PaymentProcessor.java

진행할까요?
```

#### 3. tdd-blue agent 호출

사용자 확인 후 tdd-blue agent를 **standalone 모드**로 호출:

```
[standalone] 다음 파일에 Composed Method 지향 Tidying Process를 적용해주세요:
- src/main/java/com/example/OrderService.java
- src/main/java/com/example/PaymentProcessor.java

8단계 Tidying Process (Guard Clauses → One Pile → Reorder → Chunk → Comment → Extract → Domain Logic → Trimming)를 순서대로 적용하고,
변경이 있으면 하나의 refactor: 커밋으로 완료해주세요.
```

#### 4. 결과 보고

tdd-blue agent 완료 후 사용자에게 결과 보고:
- 적용된 tidying 단계
- 변경된 파일과 주요 개선 사항
- 커밋 해시 (변경이 있는 경우)

## FAILURE CONDITIONS

- ❌ 테스트 파일을 tidying 대상에 포함
- ❌ 동작이 변경되어 테스트가 실패
- ❌ 사용자 확인 없이 tidying 진행
- ❌ git add -A로 전체 파일 추가
```

- [ ] **Step 3: 파일 생성 확인**

Run: `cat msbaek-tdd/skills/tdd-tidy/SKILL.md | head -5`
Expected: frontmatter의 name: tdd-tidy 확인

- [ ] **Step 4: 커밋**

```bash
git add msbaek-tdd/skills/tdd-tidy/SKILL.md
```
Write tool로 커밋 메시지 파일 생성:
```
feat(tdd): tdd-tidy 독립 tidying 스킬 추가
```
```bash
git commit -F /tmp/commit-msg.txt && rm /tmp/commit-msg.txt
```

---

### Task 3: plugin.json 버전 범프

**Files:**
- Modify: `msbaek-tdd/.claude-plugin/plugin.json`

- [ ] **Step 1: plugin.json 버전 업데이트**

기존:
```json
{
  "name": "msbaek-tdd",
  "version": "1.2.0",
  "description": "Java + Spring Boot TDD workflow plugin with RGB cycle orchestration",
  "author": {
    "name": "msbaek"
  },
  "keywords": ["tdd", "java", "spring-boot", "red-green-blue"]
}
```

변경:
```json
{
  "name": "msbaek-tdd",
  "version": "1.3.0",
  "description": "Java + Spring Boot TDD workflow plugin with RGB cycle orchestration and standalone tidying",
  "author": {
    "name": "msbaek"
  },
  "keywords": ["tdd", "java", "spring-boot", "red-green-blue", "tidying"]
}
```

- [ ] **Step 2: 커밋**

```bash
git add msbaek-tdd/.claude-plugin/plugin.json
```
Write tool로 커밋 메시지 파일 생성:
```
chore(tdd): 버전 1.3.0으로 범프 — tdd-tidy 스킬 추가 반영
```
```bash
git commit -F /tmp/commit-msg.txt && rm /tmp/commit-msg.txt
```

---

### Task 4: 검증

- [ ] **Step 1: 전체 파일 구조 확인**

Run: `find msbaek-tdd -name "*.md" -o -name "*.json" | sort`
Expected:
```
msbaek-tdd/.claude-plugin/plugin.json
msbaek-tdd/agents/tdd-blue.md
msbaek-tdd/agents/tdd-green.md
msbaek-tdd/agents/tdd-red.md
msbaek-tdd/skills/tdd/SKILL.md
msbaek-tdd/skills/tdd-plan/SKILL.md
msbaek-tdd/skills/tdd-rgb/SKILL.md
msbaek-tdd/skills/tdd-tidy/SKILL.md
```

- [ ] **Step 2: tdd-blue.md에 standalone 키워드 존재 확인**

Run: `grep -c "standalone" msbaek-tdd/agents/tdd-blue.md`
Expected: 3 이상 (모드 판별, 작업 절차, 키워드 참조)

- [ ] **Step 3: SKILL.md frontmatter 확인**

Run: `head -4 msbaek-tdd/skills/tdd-tidy/SKILL.md`
Expected:
```
---
name: tdd-tidy
description: git diff 기준 변경 파일을 자동 탐지하여 Composed Method 지향 Tidying Process를 독립 실행. /tdd-tidy로 호출.
argument-hint: "[commit-ref]"
```

- [ ] **Step 4: plugin.json 버전 확인**

Run: `cat msbaek-tdd/.claude-plugin/plugin.json`
Expected: version이 "1.3.0"
